FROM node:22-bookworm-slim

WORKDIR /app

# パッケージの依存関係をコピーしてインストール
COPY package.json package-lock.json* ./
RUN npm install

# その他のファイルはdocker-composeのvolumeでマウントされるため不要
# Next.jsのテレメトリを無効化（任意）
ENV NEXT_TELEMETRY_DISABLED=1

EXPOSE 3000

CMD ["npm", "run", "dev"]
